// Re-export central types with local names for backward compatibility
export type {
  ReactionLocal as Reaction,
  SenderLocal as Sender,
  MessageLocal as Message,
  TicketDetailLocal as Ticket
} from "../../types";