// Re-export central types with local names for backward compatibility
export type {
  UserSkillLocal as UserSkill,
  UserProjectLocal as UserProject,
  UserAchievementLocal as UserAchievement,
  UserTeamLocal as UserTeam,
  UserDepartmentLocal as UserDepartment,
  UserStatsLocal as UserStats,
  UserSocialLinksLocal as UserSocialLinks,
  UserDetailLocal as User
} from "../../types";